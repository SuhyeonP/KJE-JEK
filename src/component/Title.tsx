import styled from '@emotion/styled';

const TitleStyled = styled.div`
  position: sticky;
  padding-top: 45vh;
  top: -32vh;
`;

export const Title = (): JSX.Element => {
  return (
    <TitleStyled>
      <p>icon icon</p>
    </TitleStyled>
  );
};
